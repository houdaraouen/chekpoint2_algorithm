ALGORITHM somme
VAR
 set1 : ARRAY_OF INTEGER[5];
 set2 : ARRAY_OF INTEGER[5];
 i : INTEGER;
 sum : INTEGER;
BEGIN
    set1 := {1,5,3,8,4};
    set2 := {3,5,6,8,4};
    write("Print Array:");
    sum := 0;
FOR i FROM 0 TO 4 STEP 1  DO
FOR i FROM 0 TO 4 STEP 1  DO
    IF (set1[i]=set2[i]) THEN
        sum := set1[i]+set2[i];
           i := i+1; 
    END_IF
END_FOR
END_FOR
END